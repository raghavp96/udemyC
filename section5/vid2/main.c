#include <stdio.h>

int main() {
    int x[3]; // x is populated with grabage values
    int y[3] = {0}; // y is populated with all 0 values;
    int z[3] = {1, 2, 3}; // z is populated with 1,2,3
    int w[3] = {1, 2}; // third value in w is initialized to 0 by default
    
    return 0;
}